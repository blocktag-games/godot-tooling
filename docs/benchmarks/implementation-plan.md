# Benchmark implementation roadmap

Status on 19 September 2026: protocol and catalogs drafted; BP01 through BP06 are all closed. An internal review on this date found specific unmet completion evidence in BP02-BP04 and, separately, one incorrect finding in BP04's original write-up (since corrected). All named gaps were closed the same day, including a genuine second-reviewer pass on all 16 oracles by an independent review agent, which itself found and fixed four further issues (see BP02's row). BP05 added two new real candidates (Nano Coverage, godot-code-coverage) plus a GdUnit4 driver (godot-code-coverage is currently incompatible with Godot 4.7.1; Nano Coverage's memory-mode API crashes, though its GdUnit4 session-hook integration works). BP06 built the full tier-1 correctness corpus across all eight domains, ran a second independent oracle review (five real issues found and fixed), and closed with a corpus run against gd-tools-cli 0.4.0 confirming four real tool behaviors -- see [corpus-run-2026-09-19.md](corpus-run-2026-09-19.md) and BP06's row. See [pilot-environment.md](pilot-environment.md), [../../pilot/fixtures/](../../pilot/fixtures/), [../../pilot/harness/](../../pilot/harness/), and [../../pilot/adapter-run/](../../pilot/adapter-run/). Work packages below are ordered by dependency. Estimates are planning ranges for one active contributor and will be revised after the first runnable slice.

## Work packages and completion evidence

| ID | Package | Depends on | Completion evidence | Initial effort |
| --- | --- | --- | --- | --- |
| BP01 | Freeze pilot scope and environment identity. | This draft | **Closed 2026-09-19.** [pilot-environment.md](pilot-environment.md): Godot 4.7.1 (`a13da4feb`, sha256 pinned), gd-tools-cli 0.4.0 via Pipenv, GUT v9.7.1 pinned and vendored, machine record, pilot schedule, explicit unavailable modes. | 1–2 days |
| BP02 | Implement the first independent fixtures and oracles. | BP01 | **Closed 2026-09-19.** All 16 tier-0 oracles share one schema (`pilot/harness/oracle.py`'s `load_oracle`/`obligations_for_input`/`union_obligations`), replacing four incompatible `expected_hit` shapes; validated against real BP04 evidence, including a genuinely new mechanized result (F020's `union_obligations` reproduces the "line 4 has no trackable point" finding that previously needed hand-written prose). F062 has a committed, runnable check (`cases/f062_injected_instrumentation_failure/check.py`). F008/F064/F071 stand as real, reproducible findings against the pinned tools. An independent review agent then performed a genuine second-reviewer pass on all 16 oracles -- re-hashing every referenced file, hand-tracing every input, re-running every driver/check.py/compare.py, and reading gd-tools/GUT source directly -- confirming 12 of 16 with no changes and finding 4 real issues, all fixed: F045 was missing a trackable obligation (line 13, the final post-resume statement); F060's notes and `driver_covered.gd` still repeated the "rewrites source on disk" claim already retracted in F040; F065's `subject.py` didn't actually implement the atomic rename its own description claimed (fixed to use `Path.rename()`); and F020's `measured_with_gd_tools_0_4_0` field wrongly said an obligation "matched correctly" when it mechanically scores as `missing_hits`. None of the four would have flipped a downstream conclusion; every measured claim reproduced exactly under independent re-verification. | 2–4 days |
| BP03 | Implement minimal orchestration and evidence records. | BP01 | **Closed 2026-09-19.** [pilot/harness/](../../pilot/harness/) now includes `workspace.py` (isolated per-run copies with hash-based restoration checking) and `outcome.py` (structured JSON run records), alongside the existing comparator/runner/faults modules. `comparator.py`'s `compare()` now checks each `Obligation`'s own `sha256` against current source as `oracle_stale_files`, distinct from report-vs-source `stale_files`. The null-adapter self-test now runs a real subject through two isolated workspaces via the real process runner instead of comparing two hardcoded strings; the truncated-JSON test now calls the harness's own `load_actual_report()` instead of the standard library directly. 14/14 self-tests pass, including two new ones (`oracle_stale_source_detected`, `inconsistent_oracle_hash_rejected`) and four workspace-isolation checks. | 2–4 days |
| BP04 | Add the first coverage adapter. | BP02, BP03 | **Closed 2026-09-19.** [pilot/adapter-run/](../../pilot/adapter-run/): real `gd-tools test --coverage` (gd-tools-cli 0.4.0 + GUT v9.7.1) run against F010 (clean match against its oracle, loaded via `pilot/harness/oracle.py`) and F020. F020's original write-up misdiagnosed its branch-count finding as an "unresolved anomaly"; a same-day correction, backed by three independent runs (both branches, true-only, false-only) plus reading `coverage.gd`'s injection logic, confirmed the real finding: gd-tools' `if_true` branch counter fires on every evaluation of the decision, not on the true outcome specifically, so it can report 100% branch coverage while the true branch's body never executed. `pilot/adapter-run/reproduce.sh <case>` is now the literal one-command reproduction: it restores the gitignored `addons/` (from the one already-committed GUT copy and gd-tools' installed package), clears prior `.godot`/`.gd-tools` state, rebuilds the import cache, runs `gd-tools test --coverage`, and runs `compare.py` -- verified end to end from a state with `addons/`, `.godot/`, and `.gd-tools/` actually deleted; the only diffs in regenerated evidence are nondeterministic timestamps, hit data is byte-for-byte identical. | 2–4 days |
| BP05 | Add remaining candidate modes and parity drivers. | BP04 | **Closed 2026-09-19.** [pilot/candidates/](../../pilot/candidates/) and [pilot/adapter-run/](../../pilot/adapter-run/): godot-code-coverage pinned at its actual latest commit (`3c92852d`) and confirmed **incompatible** with Godot 4.7.1 -- a real, current type-checking break in its `NullCoverage` class fails silently (tests still pass, exit 0, no coverage file ever written; see `pilot/candidates/godot-code-coverage/PIN.md`). Nano Coverage pinned at its actual latest commit (`fce7a0ae`), built successfully from source (scons, pip-installed with no sudo needed) after working around a real packaging bug (its own `.gdextension` file doesn't match what SCons actually names the build output); the extension then loads correctly on 4.7.1 despite targeting `godot-cpp`'s 4.3-branch API, but its own documented headless/CI API example crashes with a null-reference error outside the full editor-plugin context (see `pilot/candidates/nano-coverage-godot-PIN.md`). GdUnit4 v6.2.1 vendored and confirmed working cleanly against the pinned engine (`pilot/candidates/gdUnit4/PIN.md`), closing the GdUnit4-driver item. Manual/standalone driver and further GdUnit4 pairing (e.g. with Nano Coverage's session hook) not built this pass -- see BP06's row for what that means for scope. | 3–7 days |
| BP06 | Complete primary correctness corpus. | BP05 | **Closed 2026-09-19.** Confirmed Nano Coverage's GdUnit4 integration works correctly (`pilot/adapter-run/nano-coverage-gdunit4-f010_straight_line/`), narrowing BP05's finding 4 and making Nano Coverage a real, viable corpus candidate alongside gd-tools and GdUnit4-as-runner; godot-code-coverage remains not viable as shipped. Before writing tier-1 control-flow fixtures (F022 onward, nearly all branch-shaped), fixed a real schema gap: decision lines (if/elif/loop headers) are now first-class `{"kind": "branch", "branch_type": ...}` obligations in `pilot/harness/oracle.py`'s schema, retrofitted into F020/F021 and verified to mechanically detect gd-tools' branch-counter deviation via `comparator.compare()` (a `false_hit`) instead of needing hand-written prose. Tier-1 corpus now complete for population (F003-F005; F006/F007/F009 tier-2), lines (F011/F013-F016; F017-F019 tier-2), control_flow (F022-F030; F028/F031 tier-2), language (F032-F037; F038/F039 tier-2), and lifecycle (F041-F044/F046/F048/F049; F047 tier-2) -- see `pilot/fixtures/README.md` for each domain's specifics. A second-reviewer (advisor) pass caught and fixed three real defects before they propagated: an oracle line-misattribution in F034, a zero-discriminating-power pair of inputs in F013, and two attempts (F041, F044 drafts) to express a negative fact ("this never ran") as a snapshot inside a process that would later make it true -- restructured to genuinely separate fresh processes per input, the same fix both times. A corpus-wide lint (no obligation on a blank/comment/signature line; no if/while/for header reading unreached while a line in its body reads reached) also found and fixed one pre-existing violation in F030. Partial tier-1 coverage added for failures (F061/F063/F068/F069; F066/F067 need process-control infrastructure not yet built, left `planned`), merge (F070/F072), and robustness (F075/F076) -- all real end-to-end gd-tools-cli 0.4.0/GUT v9.7.1 runs following the F005/F071 pattern, not reimplementations. Two more real findings: F069 shows neither `coverage show` nor `coverage report` checks the plan's own recorded source hash against current disk state (a stale report satisfies a fresh check with no warning), and F072 shows merging identical input twice silently doubles the hit count with no duplicate detection. A gitignore gap (F076's two-sub-project layout, one directory level deeper than every other adapter-run case, wasn't matched by the existing single-level `*/addons/...` patterns) was found and fixed during this pass, then hit again identically for F080 and fixed the same way. Integration domain's tier-1 scope (F080, F081) is now built: F081 proves gd-tools' coverage collection is genuinely framework-independent by hand-replicating pre_run_hook.gd/post_run_hook.gd (both are thin GUT-specific glue over a runner-agnostic `_GDTCoverage` autoload) in a from-scratch manual driver with no GUT/gd-tools CLI at all, and getting an artifact byte-equal to the real GUT-path reference -- a real timing bug (the autoload's `_ready()`, where instrumentation happens, fires on the first idle frame, not synchronously before a driver script's `_initialize()`) was found and fixed while building it. F080 confirms GUT and GdUnit4 produce a byte-identical application-behavior trace for the same subject.gd and inputs, explicitly scoped to application behavior, not gd-tools coverage parity (which is GUT-hook-bound and untested for GdUnit4). **Tier-1 corpus is now complete** with F066/F067: F066 confirms a SIGKILL of the Godot process (simulating a crash, not a graceful interruption) leaves only pre-crash evidence (plan.json), never a fabricated coverage.json/results.xml, and gd-tools reports the real crash exit code honestly; a fresh run afterward recovers with no manual cleanup. F067 found and fixed a real defect in this project's OWN harness, not a third-party tool: `pilot/harness/runner.run()`'s timeout used plain `subprocess.run(..., timeout=...)`, which only kills the direct child -- the Godot process gd-tools spawns as its own child was left orphaned and running after the harness's timeout fired, confirmed via direct process inspection. Fixed by launching in a new process session (`start_new_session=True`) and killing the whole process group (`os.killpg()`) on timeout; verified against both the real gd-tools/Godot scenario and a new fast synthetic self-test (`pilot/harness/self_test.py`'s `test_run_kills_entire_process_tree_on_timeout`), which itself needed a small fix (poll for process death instead of checking once, since SIGKILL delivery isn't synchronous with the kill syscall returning -- an initial flaky failure was a real timing bug in the test, not the fix). **Oracle review record closed 2026-09-19:** a second independent review agent reviewed the 40 oracles the first pass never saw (all tier-1 oracles plus F045, fixed by the first pass but left with a null `reviewer` field) -- re-hashing every bound file, checking every obligation's line against source, hand-tracing every pure-GDScript input, re-running every driver and every real-tool check.py (all reproduce exactly), and probing the lifecycle-domain negative claims with instrumented copies on the pinned engine. 35 of 40 confirmed clean. Five real issues found and fixed: (1) F048's `shutdown_before_drain` obligations were factually wrong -- `quit()` only requests exit and Godot still runs one full main-loop iteration (deferred flush, delete-queue drain, `_process`) before honoring it, so the deferred call and PREDELETE branch DO run after the driver's print; line 9 was additionally reached at `add_child()` time for ordinary notifications. The oracle now records that pending work is included, and the driver prints from `_finalize()` so the fixture observes it. This is the third instance of the "one input = one measurable run" bug class, this time in the opposite direction (later process state falsified a negative claim), and the first not caught before commit. (2) F044's negative obligation is correct but its stated mechanism was wrong (the callback is undelivered because the RefCounted target is freed when `_initialize()` returns, not because no frame turns; holding the subject alive makes it fire). (3) F049 had a `const ... = preload()` declaration line as a statement obligation in both inputs, a convention-1 violation with zero discriminating power. (4) F067 bound `pilot/harness/runner.py` via a path with one `..` too many that resolved to nothing (hash already correct). (5) F075's notes still called F066 unbuilt. Every other measured claim reproduced under independent re-verification; `reviewer` fields now record what was checked per oracle. **Corpus run against candidate tools, closing BP06:** see [corpus-run-2026-09-19.md](corpus-run-2026-09-19.md) and `pilot/adapter-run/corpus-sweep/`. Every Group 1 (pure-GDScript) fixture with a built oracle -- 26 fixtures, 53 named inputs -- run against real gd-tools-cli 0.4.0, one invocation per input via `gd-tools test --coverage --test <func>` (never batched, same fresh-process-per-input rule established during fixture construction). 30/53 matched exactly; the other 23 all trace to four confirmed real gd-tools behaviors: (1) `if_true`/`elif_true`/`loop_body` counters fire on every evaluation of the decision, not the labeled outcome specifically (BP04's original finding, now confirmed corpus-wide and extended to loop headers -- 13 of 23 mismatches); (2) the flip side -- `elif_true`/`match_case`, being taken-only counters injected inside their own body, cannot distinguish "never evaluated" from "evaluated and false," confirmed as a genuine tool limitation, not just the schema gap flagged when F022 was built; (3) three declaration/header lines (F020 `var outcome: int`, F027's `match` header, F035's member initializer) have no trackable point in gd-tools' plan at all, a real completeness gap the comparator correctly scores as `missing_hits`; (4) ternary expressions have zero branch-level tracking, confirming F029's design intent as real tool behavior. The sweep script itself went through three iterations, catching two of its OWN translation bugs (branch-type entries not populating the parallel statement fact; a `loop_body`/`loop_entered` branch_type naming mismatch between gd-tools' vocabulary and this corpus's) before trusting any result as a finding about gd-tools rather than the script. One Group 2 (lifecycle) fixture (F042) spot-checked by hand-wiring gd-tools into a scratch copy of its own mini-project: matched exactly in a live scene-tree context. Nano Coverage's one working integration re-verified passing (4/4 checks). Explicitly NOT swept, and said so rather than assumed clean: the remaining ~10 Group 2 fixtures (each needs individual hand-wiring, not built for all of them), gd-tools coverage collection under GdUnit4 (never wired, GUT-hook-bound), and Nano Coverage across the rest of the corpus (needs a second full adapter). | 4–8 days |
| BP07 | Implement deterministic performance workloads and pilot. | BP04; selected BP06 cases | Fixed input/behavior checks, phase boundaries, matched baselines, pilot variance and cost report. | 3–6 days |
| BP08 | Validate profiling and measure the harness. | BP03, BP07 | Null/CPU/wait/log/memory controls, independent timing cross-check, one useful script/native/harness capture where available, pilot observer matrix. **Done 2026-09-20:** `pilot/performance/harness_control/validate_harness_timing.py` recovers known-magnitude ground truth (floor, wait interval, CPU/memory scaling, log-volume capture) through the real timing primitive; `pilot/performance/independent_timing_check.py` cross-checks it against an independent external timer (`/usr/bin/time -v`) on both harness-control fixtures and real multi-process candidate runs, agreeing within noise; `pilot/performance/harness_profile.py` provides the one available capture (native `perf` remains unavailable this session -- see the risk table below -- so this is a `cProfile` capture of the Python harness's own orchestration, per profiling-protocol.md's layer table) plus a storage-cost estimate; `docs/benchmarks/pilot-observer-matrix.tsv` records the reduced H/C factorial (P unavailable, documented) for W01/W03/W11, with the H effect spot-checked at -0.45% (negligible, consistent with the harness's timed window excluding all Python-side bookkeeping by construction). | 3–6 days |
| BP09 | Freeze main study and analysis. | BP06–BP08 | Tagged protocol and oracles, eligible cells, exact repetition/analysis rules, validated analysis on synthetic known data. | 1–3 days |
| BP10 | Run controlled Linux study. | BP09 | Complete schedule/attempt accounting, raw artifacts, environment checks, independent sessions, generated result tables. **Blocked on machine readiness as of 2026-09-20 -- not started.** Direct check found the machine did not match the described "quiet" state: CPU governor still `powersave`, Firefox running, an unrelated project's test runner and four of its background services live. None of this was touched (no sudo for the governor; killing another project's live processes or a browser without explicit sign-off is not this session's call to make). Before BP10 begins: governor set to `performance` (needs the user's sudo), browser/other terminal processes/other projects' services confirmed stopped, and a concrete, actually-measurable quiescence check run immediately before each session -- idle CPU% below a stated threshold for a stated duration, since this machine has no confirmed thermal telemetry to check instead (verified: `upower` exposes battery state only, not temperatures). Calendar-time amendment (performance-protocol.md, 2026-09-20, before any BP10 data collected): the original ≥3-day session spread is dropped for this single dedicated machine; sessions may run consecutively within one sitting, but each session boundary needs the idle-CPU check above plus the existing statistical-analysis requirement to inspect time-order plots for drift and report it as a limitation if found -- consecutive same-day sessions are more exposed to that risk than a multi-day spread was, and the amendment does not relax the independence assumption the analysis places on session means. | 3–5 calendar days plus machine time |
| BP11 | Verify debugger workflow. | BP05, BP06 | Godot Editor's built-in debugger (primary, dominant real-world workflow): launch/attach, verified original-source breakpoints, stepping/state, failed-test path, coverage navigation. VS Code's `godot-tools` extension (secondary, optional): same checks via its DAP-based debugger, if pursued. Revised 2026-09-20 -- earlier drafts targeted PyCharm/JetBrains tooling, dropped entirely: the Godot Editor's own integrated debugger is what the large majority of GDScript developers actually use, and F057 (instrumentation shifts reported error lines) is directly testable against it without any external-IDE bridge layer in between. | 2–5 days |
| BP12 | Add OS and machine replication. | BP06, BP09; hardware access | **Descoped 2026-09-20 -- explicit scope decision, not a pending gap.** This study targets one Linux workstation by design; the goal is an objective evaluation of the coverage tooling, not a cross-platform release engineering effort, and no second machine or OS is available or planned. BP13's published limitations section states this plainly (Linux-only, single machine) rather than treating it as unfinished work. If OS/machine access is ever obtained, this row can be reopened; nothing here should be read as inconclusive pending future replication. | Descoped |
| BP13 | Write, review, and publish the study. | BP10, BP11 | Rebuildable report, complete artifacts, a limitations section stating Linux/single-machine scope plainly (BP12 descoped by decision, not incomplete), maintainer review material, independent reproduction record, architecture decision. | 3–6 days plus optional review window |

These ranges are not a promised delivery date and do not include unlimited upstream debugging, new engine builds, or waiting for hardware/reviewers. Some work can overlap operationally, but no multi-person staffing is assumed. The first usable contribution is BP04, not completion of every catalog case.

## First executable slice

Start with gd-tools/GUT because the survey identified an integrated CLI workflow; if the actual pinned setup fails, retain that evidence and move to the next feasible candidate. This is an evaluation order, not a product preference. No adapter should require rewriting the application fixture to suit the collector.

Implement the tier-0 catalog (15 cases; see [fixture-catalog.tsv](fixture-catalog.tsv)): never-loaded file, uncalled function, simple line execution, multiline expression, both and implicit branch outcomes, for-loop iteration/exhaustion, instrumentation-cannot-proceed on a file the collector's parser rejects, autoload initialization ordered both before and after the collector autoload, awaited continuation, deterministic state preservation across a script reload, zero requested tests, graceful interruption, and source-revision-aware merge including a plan-identity mis-attribution variant. Some fixtures need multiple named input runs; count those runs explicitly.

Build cases in three groups so BP02's estimate isn't consumed by harness-dependent cases before the harness exists: (1) pure GDScript cases needing only a standalone driver — never-loaded file, uncalled function, straight-line body, both branch outcomes, multiline expression, for-loop iteration; (2) cases needing a real Godot process lifecycle — deterministic state preservation, both autoload-ordering variants, awaited continuation; (3) cases needing BP03's harness — instrumentation-cannot-proceed, zero requested tests, graceful interruption, source-revision mismatch.

Build one baseline-to-report path with source hashes, a native report, normalized obligation differences, and a readable result record. Prove a false hit, missing file, stale report, or truncated report cannot silently become a successful complete measurement in our own harness using synthetic adapter inputs. These tests protect the evidence pipeline.

The first public pilot should let a reader run a small correctness case and rebuild its comparison. It should also show an empty-run timing control and one substantive workload. Do not wait for a dashboard, cross-platform installer, or a new coverage engine.

## Stage the matrix rather than multiply every factor

The [experiment matrix](experiment-matrix.tsv) identifies experiment families. Expand selected families into exact tool/mode/workload/engine/platform cells in BP01 for the pilot and BP09 for the main study. Every expanded cell needs an ID, eligibility rule, schedule, and disposition. The matrix is not an instruction to run the full Cartesian product.

Initial main-performance scope is W01, W03, W05, and W11 on Linux with three open candidate configurations where feasible. Use one supported runner/mode per candidate for that primary set; additional modes and scaling are separate extensions. Keep the partly closed comparator's within-limit feature checks and boundary cases separate from unrestricted scaling. Exceeding a published file limit must not be used as evidence of ordinary scalability failure.

For a planning example with three candidates, four workloads, ten sessions, six blocks per session, and three B0/B1/C conditions, the primary set is `3 × 4 × 10 × 6 × 3 = 2,160` process invocations. With only B0/C it is 1,440. This excludes pilots, fresh imports, profiling, repeats after invalid attempts, and other runners/platforms.

At an illustrative 5 seconds per process, 2,160 invocations alone take 3 hours; at 15 seconds, 9 hours. Preparation, imports, capture, validation, thermal/session spacing, and analysis add time. These are arithmetic budget scenarios, not measured runtimes. BP07 must produce the real budget before BP09 freezes the schedule.

An eight-condition observer experiment costs `workload/collector/profiler combinations × sessions × blocks × 8` invocations. A three-workload, one-collector, one-profiler pilot with two sessions and five blocks costs 240 launches; the ten-session/six-block version costs 1,440. Select the main-study combinations based on explicit methodological questions, not whichever pilot result looks most dramatic.

Large traces may dominate storage. Record bytes per capture in BP08, estimate the planned total plus retention headroom, and choose a versioned archive before the main study. Keep source/analysis in Git and bulk artifacts outside ordinary Git history.

## Proposed implementation layout

When BP02–BP04 begin, create fixtures grouped by case ID, reviewed oracle files, thin runner drivers, a harness package, provider adapters, schema validators, workloads, analysis scripts, and reproduction documentation. Keep the current planning documents in `docs/benchmarks/`. Avoid empty scaffold directories until they have useful content.

Use routine CI for schema/link integrity, harness failure controls, a small baseline/correctness subset, and report regeneration from small checked-in examples. Pin engine/tool dependencies. Hosted CI performance remains a smoke signal, not a public speed ranking. Native extension builds and IDE automation can join the matrix when reproducible and maintained.

## Decisions and risks

| Item | Current planning position | Resolve by |
| --- | --- | --- |
| Hardware and OS access | Decided 2026-09-20, not merely unconfirmed: Linux only, this one development workstation only, for the study's full duration -- a deliberate scope choice (an objective tooling evaluation, not a cross-platform release effort), not a resourcing gap awaiting resolution. See BP12. | BP01; closed by BP12's descope |
| Engine target | Godot 4.7.1 reference; exact binary/build still to pin. | BP01 |
| Candidate installation budget | Initially one working day per configuration before recording unresolved setup and moving on; publish actual effort and follow-ups. | BP01 |
| Oracle ambiguity | Record alternative contracts and defer universal claims; never derive truth from a favored collector. | BP02/BP06 |
| Missing B1 or phase controls | Mark unavailable; measure complete workflow with explicit confounds. | BP04/BP07 |
| Patched/closed comparator | Optional feature comparison; obtain exact permitted binaries and within-limit scope. | BP05 |
| Profiler access/build effort | Linux perf first if available; Godot/script and Python captures where feasible; Tracy is a separate build experiment. | BP08 |
| Workload sizes and sample counts | Proposed defaults only; choose using pilot variance and cost, then freeze. | BP09 |
| Public dataset hosting | GitHub release assets or a versioned archive, with hashes; provider undecided. | BP09/BP13 |
| Debugger under instrumentation | Godot Editor's built-in debugger is primary and required; F057 already shows instrumentation shifts reported error lines, so whether breakpoint binding is similarly affected is a real open question, not assumed either way. JetBrains/PyCharm dropped from scope 2026-09-20 -- not the dominant real-world GDScript workflow. VS Code's `godot-tools` extension (confirmed 2026-09-20: an official godotengine-org project, real DAP-based launch/attach/breakpoints/stepping/variables) may be added as an optional secondary target. | BP11 |
| Independent review | Oracle review and reproduction participation to recruit when artifacts exist. | BP06/BP13 |
| New tool architecture | Remains open pending evidence; upstream contribution is a valid outcome. | BP13 |

Document protocol amendments with date, affected question/cells, reason, whether data had been seen, and effect on interpretation. Preserve failed approaches as evidence only when they explain a result or limitation; keep the public instructions centered on the final reproducible workflow.
